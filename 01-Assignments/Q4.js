// Given the string "JavaScript", log its length,
//  its first character, its last character, and the whole word in uppercase.

const str = "JavaScript";
console.log(str.length)
console.log(str[0])
console.log(str[str.length - 1])
console.log(str.toUpperCase())