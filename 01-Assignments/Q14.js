// "  Hello World  ". Log it trimmed, its length before and after trimming, and whether it includes "World".

const str = "    Hello World    "

console.log(str.length)
console.log(str.trim().length)
console.log(str.includes("World"))